# ShopEjs

EJS-based Node.js shop application.

## Environment

Create a `.env` file with the following:

```
STRIPE_SECRET_KEY=your_stripe_secret_key
SENDGRID_API_KEY=your_sendgrid_api_key
```

## Setup

```bash
npm install
npm start
```
